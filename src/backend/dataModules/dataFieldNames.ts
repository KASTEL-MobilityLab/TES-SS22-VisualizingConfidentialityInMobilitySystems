/**
 * VehicleDataFieldTypes defines the specifc type of a User DataField that is display to the user.
 */
export enum UserDataFieldNames {
  forename = "Forename",
  surname = "Surname",
  phoneNumber = "Phone Number",
  email = "E-Mail",
}
